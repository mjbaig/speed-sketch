# Speed Sketch App

This is mostly AI generated. I just wanted a way to time myself while sketching.

## How to run

- Download nodejs lts
- Clone the repo
- npm install in the directly that you cloned
- make a directly called public
- put all the images you want to speed sketch in public. It doesn't matter if they're in nested directories
- run npm start in the terminal
- go to localhost:3000
